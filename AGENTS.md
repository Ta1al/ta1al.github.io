# Repository instructions

- Create a Git commit after each significant, coherent change.
- Keep commits focused and use descriptive conventional-style messages.
- Run the Hugo production build and `git diff --check` before committing implementation changes.
- Do not commit generated Hugo output or caches.
